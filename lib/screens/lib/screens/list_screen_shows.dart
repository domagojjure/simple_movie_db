import 'package:flutter/material.dart';

class ListScreenShows extends StatelessWidget {
  static const routeName = '/list-screen-shows';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('top shows')),
    );
  }
}
